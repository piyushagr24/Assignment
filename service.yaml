apiVersion: v1
kind: Service

metadata:
  name: devops-assignment-service

spec:
  selector:
    app: devops-assignment

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080

  type: NodePort